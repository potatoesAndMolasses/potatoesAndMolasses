/******************************************************************************

Faca um programa que peca ao usuario para digitar 10 valores e some-os

*******************************************************************************/
#include <stdio.h>

int main() {
    
    /*
    int valores = 1;
    int soma, numeros_digitados = 0;
    
    while (valores <= 10) {
        
        printf("Digite o %d° valor a ser somado:\n", valores);
        scanf("%d", &numeros_digitados);
        soma += numeros_digitados;
        valores++;
    }
    printf("A soma de todos os valores digitados é: %d", soma);
    */
   
   /* 
    int quantidade_numeros = 10;
    int soma, valor = 0;
    
    printf("Vamos somar %d, valores.\n", quantidade_numeros);
    
    for(int i = 0; i < quantidade_numeros; i++) {
        printf("Digite o %d valor:\n", i + 1);
        scanf("%d", &valor);
        soma += valor;
    }
    printf("A soma dos valores é: %d", soma);
    */
    
    return 0;
}

